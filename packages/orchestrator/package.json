{
  "name": "@openclaw-middleware/orchestrator",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@openclaw-middleware/obsidian-bridge": "workspace:*",
    "@openclaw-middleware/tiered-memory": "workspace:*",
    "@openclaw-middleware/sigma-verifier": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
